import React, { FC } from 'react';

import classes from './StoryWrapper.module.scss';

const StoryWrapper: FC = () => {
  return (
    <div className={classes.container}>
      <div></div>
    </div>
  );
};

export default StoryWrapper;
